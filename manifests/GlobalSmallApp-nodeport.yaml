apiVersion: v1
kind: Service
metadata:
  name: globalnodeapp-nodeport
spec:
  type: NodePort
  selector:
    app: globalnodeapp
  ports:
  - port: 80
    targetPort: 8080
    nodeport: 30049
    name: http80
    