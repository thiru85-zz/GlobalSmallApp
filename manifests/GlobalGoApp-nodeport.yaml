apiVersion: v1
kind: Service
metadata:
  name: globalgoapp-nodeport
  labels:
    app:
spec:
  type: NodePort
  selector:
    app: globalgoapp
  ports:
  - port: 81
    targetPort: 8081
    name: http81